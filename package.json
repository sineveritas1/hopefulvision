{
  "name": "hopeful-vision",
  "version": "1.0.0",
  "private": true,
  "description": "Hopeful Vision: a WebGL2 particle toy at hopeful.vision",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "tail": "wrangler pages deployment tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
